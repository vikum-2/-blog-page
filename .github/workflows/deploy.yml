name: Deployment Documentation

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment Info
        run: |
          echo "Deployment Provider: Netlify"
          echo "Deployment Method: Auto-deploy from main branch"
          echo "Build Commands: None (static site)"
          echo "Output Directory: src/"
